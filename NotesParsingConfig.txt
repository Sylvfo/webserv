do the canonical class for WebServ

initErroCode(servers[i]);
initMimeTypes(servers[i]); a mettre dans le constructeur? Demande à Sylvie

if servers on the same port?


HTTP (the web) is port 80, telnet is port 23, SMTP is port 25, the game
DOOM17 used port 666, etc. and so on. Ports under 1024 are often considered
special, and usually require special OS privileges to use.

data in private
some stuff in public
