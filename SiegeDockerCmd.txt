docker run --rm --network=host lopezs/siege -t1M -c2 -b http://ip:port

create a file ~/siege-sites.txt
Then :
docker run --rm --network=host -v ~/siege-sites.txt:/siege-sites.txt lopezs/siege -t10s -c100 -f /siege-sites.txt

siege options:
-t10S = temps
-r how many times (requests but I think not per user)
-c users


ifconfig pour trouver ip

ou mettre localhost??

-b flag = aucun délai entre les requetes

sur ordi syl:
docker run --rm --network=host lopezs/siege -t5M -c100 -b http://localhost:2223
ok fonctionne

Max clients:
//limit par processus
ulimit -n
Usually you can have max 1024 fd per process. Some other fd are needed... so let's take 80% of them
800 (evtl 900)

Max request:


//pour savoir combien de fd sont ouverts pendant que siege tourne
pidof webserv
ls /proc/PID/fd


//fd are closed at the end of the programm. But let's pause it juste before. 
//au demarage
ls -l /proc/PID/fd
//a la fin
ls -l /proc/PID/fd

//verifier zombies.
ps aux | grep defunct

//memory used
top -p $(pidof mon_programme)